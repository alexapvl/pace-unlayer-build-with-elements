# Pace v0 evidence

This review pack was generated from the real Pace baseline run represented by commit
[`a029f46`](https://github.com/alexapvl/pace-unlayer-build-with-elements/commit/a029f46ff633d8e79b490e9b62c3269b8300835b).

- `run.toon` is the compact, lossless agent context.
- `design.json` is the Unlayer-compatible document design.
- `report.pdf` is the rendered review document.
- `screenshot.png` shows the run inside the Pace review workspace.

The baseline contained 19 files and 4,506 additions. Verification completed with 12 tests,
TypeScript checking, a production build, TOON round-trip checks, and manual browser review.

Regenerate the full artifact set with:

```sh
pnpm verify
pnpm render:pdf
```
